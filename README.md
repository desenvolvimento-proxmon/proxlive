# PROXLIVE

Site em Next.js para câmeras ao vivo, com Home, detalhe por câmera e dados cadastrados direto no código.

## Rodando localmente

```bash
npm install
npm run dev
```

As câmeras, banners, propagandas, links sociais e botão de WhatsApp ficam em `src/lib/proxlive-data.ts`.
